# Session-mode pooler (port 5432). Percent-encode special characters in the password.
# NEVER give this to apps/web or apps/mobile: it connects as `postgres` and
# bypasses RLS entirely.
DATABASE_URL=postgresql://postgres.kevynftebdaoidljydyf:[YOUR-PASSWORD]@aws-0-eu-west-2.pooler.supabase.com:5432/postgres

LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# ADR-0002: every stage of the pipeline is a config value, so a model swap is
# an env var rather than a refactor.
VOXI_STT_MODEL=nova-3
VOXI_LLM_MODEL=claude-haiku-4-5
VOXI_TTS_MODEL=sonic-2
DEEPGRAM_API_KEY=
ANTHROPIC_API_KEY=
CARTESIA_API_KEY=
